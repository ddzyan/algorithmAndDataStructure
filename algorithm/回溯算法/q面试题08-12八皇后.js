/**
 设计一种算法，打印 N 皇后在 N × N 棋盘上的各种摆法，
 其中每个皇后都不同行、不同列，也不在对角线上。
 这里的“对角线”指的是所有的对角线，不只是平分整个棋盘的那两条对角线。
*/

/**
 *
 * @param {number} row 当前行数
 */
const cal8queens = function (row) {};

/**
 * @description 判断 row 行 column 列放置是否合适
 * @param {number} row 行
 * @param {number} column 列
 * @returns {boolean}
 */
const isOk = function (row, column) {};

/**
 * @description 打印一个二位矩阵
 * @param {Array} res
 * @returns {void}
 */
const printQueens = function (res) {};
