// 采用 rk hash计算方式进行字符串匹配

const hasCode = function (str) {};

/**
 * @description:找到匹配字符串的起始位置
 * @param {string} str 进行匹配的模板字符串
 * @return {number} 返回匹配字符串的起始位置
 */
const indexOfB = function (str) {};

String.prototype.indexOfB = indexOfB;
