// 在有序不重复数组中查找第一个大于或者等于指定值的数值下标

/**
 *
 * @param {Array} arr
 * @param {number} low
 * @param {number} hight
 * @param {number} value
 */
const binarySearch = function (arr, low, hight, value) {};

/**
 *
 * @param {Array} arr
 * @param {number} value
 */
const search = function (arr, value) {
  const len = arr.length;

  binarySearch(arr, 0, len - 1, value);
};
